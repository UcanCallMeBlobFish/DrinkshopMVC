@using Drinkshop.ViewModel
@model DrinkViewModel

<div>
    <h2>All Drinks are shown here</h2>
</div>

<h2>@Model.CurrentCategory</h2>

<div class="row">
    @foreach (var drink in Model.Drinks)
    {
       @Html.Partial("DrinkTable", drink);
    }
</div>
